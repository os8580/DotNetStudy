# Topic6 Ч ƒженерики (Generics) Ч “еори€ и примеры

÷ель: объ€снить, как работают обобщени€ в C#, зачем они нужны и какие ограничени€ бывают.

Ћекционный материал
1. «ачем нужны дженерики
- ”стран€ют необходимость приведени€ типов и повышают безопасность типов во врем€ компил€ции.
- ѕозвол€ют писать переиспользуемый код (списки, репозитории, утилиты).

2. —интаксис
- `class GenericBox<T> { public T Content { get; set; } }`
- ќбобщЄнные методы: `public T Create<T>() where T : new() { return new T(); }`

3. ќграничени€ (constraints)
- `where T : class` Ч ссылочный тип.
- `where T : struct` Ч значимый тип.
- `where T : new()` Ч наличие публичного конструктора без параметров.
- `where T : BaseType` Ч наследование от `BaseType`.

4. ѕримеры использовани€
- `Repository<T>` дл€ хранени€ объектов доменной модели.
- `Factory<T>` с ограничением `new()` дл€ динамического создани€ объектов.

QA-подходы
- —оздавайте обобщенные утилиты дл€ обработки данных тестов (например, `Cache<T>`, `CsvParser<T>`).
- ќграничивайте `T` там, где нужен конкретный функционал (например, `where T : IIdentifiable`).

ѕрактические задачи
- –еализовать `Repository<T>` и продемонстрировать добавление объектов разных типов.
- ѕоказать `Factory<T>` с `new()` ограничением и создание простых типов.

‘айлы:
- `GenericBox.cs`
- `GenericConstraintsDemo.cs` (новый файл с демонстрацией ограничений)
- `Program.cs`
