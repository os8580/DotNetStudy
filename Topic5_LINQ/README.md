# Topic5 Ч LINQ (“еори€, примеры и советы)

÷ель: объ€снить принципы LINQ, показать основные операции и отличи€ между отложенным и немедленным выполнением запросов.

Ћекционный материал
1. „то такое LINQ
- LINQ Ч €зык запросов, встроенный в C#, работающий поверх `IEnumerable<T>` и `IQueryable<T>`.
- «апросы LINQ состо€т из операторов преобразовани€ и фильтрации: `Where`, `Select`, `OrderBy`, `GroupBy`, `Join` и т.д.

2. Deferred vs Immediate Execution
- Deferred: `Where`, `Select` не выполн€ют запрос немедленно Ч выполнение происходит при итерации (`foreach`) или при вызове методов, которые заставл€ют материализацию (`ToList()`, `ToArray()`, `Count()`, `First()`).
- Immediate: вызов `ToList()` или `Count()` материализует результат и создает snapshot.

3. ќсновные операторы с примерами
- `Where` Ч фильтраци€.
- `Select` Ч проекци€.
- `OrderBy`/`OrderByDescending` Ч сортировка.
- `GroupBy` Ч агрегаци€ по ключу.
- `Join` Ч объединение двух наборов по ключу.
- `First`/`FirstOrDefault` Ч вз€ть первый элемент.
- `Any` Ч проверка существовани€.

4. Ёффективность и best practices
- ѕредпочитайте `Count(predicate)` вместо `Where(...).Count()`.
- »збегайте многократной материализации коллекций (несколько `ToList()` подр€д).
- ѕри необходимости сложных вычислений комбинируйте фильтрации до проектировани€ результата, чтобы сократить объем данных.

QA-ориентированные советы
- ƒл€ поиска элементов на странице используйте `FirstOrDefault` и обрабатывайте `null` безопасно через `?.` и `??`.
- ƒл€ подсчета встречаемости текста используйте `GroupBy` + `Count()`.
- ѕример: `elements.Where(e => e.Displayed && e.TagName == "button").Select(e => e.Text)`.

ѕрактические задани€
- Ќаписать LINQ-запрос, который группирует элементы по `TagName` и возвращает топ-3 групп по количеству.
- ƒемонстраци€ deferred vs immediate: собрать snapshot и показать, что последующие изменени€ исходной коллекции не вли€ют на snapshot.

‘айлы:
- `Program.cs`
- `WebElement.cs` (модель элемента страницы)
